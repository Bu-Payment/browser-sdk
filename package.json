{
  "name": "@bu-payment/browser-sdk",
  "version": "0.1.0",
  "description": "Browser-safe BuPayment client for public catalogue and hosted checkout flows.",
  "type": "module",
  "sideEffects": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bu-Payment/browser-sdk.git"
  },
  "homepage": "https://github.com/Bu-Payment/browser-sdk#readme",
  "bugs": {
    "url": "https://github.com/Bu-Payment/browser-sdk/issues"
  },
  "keywords": [
    "bupayment",
    "browser",
    "payments",
    "checkout",
    "typescript"
  ],
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "module": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:consumer": "node scripts/installed-consumer.mjs",
    "inspect:bundle": "node scripts/inspect-bundle.mjs",
    "pack:check": "npm pack --dry-run",
    "check": "npm run lint && npm run typecheck && npm run test && npm run test:coverage && npm run build && npm run inspect:bundle && npm run test:consumer"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@vitest/coverage-v8": "^3.2.7",
    "tsup": "8.5.0",
    "typescript": "5.9.2",
    "vitest": "^3.2.7"
  },
  "publishConfig": {
    "access": "public"
  }
}
